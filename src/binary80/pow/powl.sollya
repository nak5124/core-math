n = 7;

pretty = proc(u) {
  return ~(floor(u*1000)/1000);
};

d = [-1/2^10, 1/2^10];
f = log2(1 + x)/x;
w = 1;
p = remez(f, n, d, w);
pf = fpminimax(f, n, [|106, 106, 106, 53, 53, 53, 53, 53|],
	d, absolute, floating, 0, p);
err_p = -log2(dirtyinfnorm(pf*w-f, d));
print( pretty(err_p));
display = hexadecimal;
print(pf);
