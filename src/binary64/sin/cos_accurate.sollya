pretty = proc(u) {
  return ~(floor(u*1000)/1000);
};

d = [0,1/256];
f = 1;
w = 1/cos(2*pi*x);
p = remez(f, [|0,2,4,6,8,10,12,14|], d, w);
pf = fpminimax(f, [|0,2,4,6,8,10,12,14|], [|128,128,128,128,64...|], d, relative, floating, 0, p);
err_p = -log2(dirtyinfnorm(pf*w-f, d));
display = hexadecimal;
print (pf);
display = decimal;
print (pretty(err_p));
